import React from 'react';
import ReactDOM from 'react-dom/client';
import Routees from './RoutingApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Routees />
  </React.StrictMode>
);
